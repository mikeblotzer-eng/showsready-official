{
  "name": "restoremap",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-first field app for water damage restoration technicians. Static ES modules, no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  }
}
